async function load(){const r=await fetch('data/illustrations.json',{cache:'no-store'});if(!r.ok)throw new Error('catalog unavailable');return (await r.json()).illustrations||[]}function esc(s=''){return s.replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]))}function render(items){const grid=document.getElementById('grid'),empty=document.getElementById('empty'),count=document.getElementById('count');count.textContent=`${items.length} record${items.length===1?'':'s'}`;if(!items.length){empty.hidden=false;return}grid.innerHTML=items.map(x=>`<a class="card" href="${esc(x.url||'#')}"><img class="art" src="${esc(x.image||'assets/placeholder.svg')}" alt="${esc(x.title||x.id)}"><div class="body"><div class="meta">${esc(x.id||'')}</div><h3>${esc(x.title||'Untitled illustration')}</h3><p class="desc">${esc(x.description||'')}</p><div class="doi">${x.doi?`DOI: ${esc(x.doi)}`:'DOI pending'}</div></div></a>`).join('')}load().then(render).catch(()=>{document.getElementById('empty').hidden=false;document.getElementById('count').textContent=''});document.getElementById('year').textContent=new Date().getFullYear();
